import './store.js'

/*function fetchSingle(id) {
    const single = type {import('./types').show}
    if (single) {
        
    }
}*/

/**
* @type {import('./types.ts').season[]}
*/

const singleSeason = this.season;

const seasonAttr = singleSeason.map(
  ({ season, title, image, episode }) => {

    switch (number) {
      case number:
        return season.number;

      default: throw new Error('cannot find "season"');
    }
  }
)